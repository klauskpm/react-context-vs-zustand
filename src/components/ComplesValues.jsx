function ComplexValues({ unchanged, count }) {
    return (
        <div>
        <p>Complex value:</p>
        <ul>
            <li>{unchanged}</li>
            <li>{count}</li>
        </ul>
        </div>
    );
}

export default ComplexValues;